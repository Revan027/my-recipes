import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.morganfreyss.myRecipes',
  appName: 'my-recipes',
  webDir: 'dist/my-recipes/browser'
};

export default config;
